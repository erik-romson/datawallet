spring:
  datasource:
    url: jdbc:tc:postgresql:16:///datawallet
    username: test
    password: test
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

datawallet:
  web-origin: https://web.example.com
