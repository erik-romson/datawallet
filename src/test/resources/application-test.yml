spring:
  datasource:
    url: jdbc:tc:postgresql:16:///datawallet
    username: test
    password: test
    hikari:
      maximum-pool-size: 5
      minimum-idle: 1
  jpa:
    hibernate:
      ddl-auto: validate
  flyway:
    enabled: true

datawallet:
  web-origin: https://web.example.com
